<p style="color: green"><%= notice %></p>

<%= render @loan %>

<div class="p-3">
  <div class="p-3">
    <%= link_to "Voltar", loans_path, :class => "btn btn-primary" %>
    <%= link_to "Editar", edit_loan_path(@loan), :class => "btn btn-primary" %>
  </div>
  <%= button_to "Apagar Empréstimo", @loan, method: :delete, :class => "btn btn-danger" %>
</div>
