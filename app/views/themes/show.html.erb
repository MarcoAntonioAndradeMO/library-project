<p style="color: green"><%= notice %></p>

<div class="ms-2">
  <%= render @theme %>
</div>

<div class="p-3">
  <div class="p-3">
  <%= link_to "Editar", edit_theme_path(@theme), :class => "btn btn-primary" %>
  <%= link_to "Voltar", themes_path, :class => "btn btn-primary" %>
  </div>
  <%= button_to "Apagar Tema", @theme, method: :delete, :class => "btn btn-danger" %>
</div>
